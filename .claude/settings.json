{
  "enabledPlugins": {
    "superpowers@claude-plugins-official": true
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read f; case $f in *.py) uv run ruff format $f && uv run ruff check --fix $f;; esac; } 2>/dev/null || true",
            "statusMessage": "Formatting and linting..."
          }
        ]
      }
    ]
  }
}
